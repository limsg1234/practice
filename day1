가운데 글자 가져오기

class Solution {
  public String solution(String s) {
      String answer = "";
      int len = s.length();
      int point = len / 2;
      if (len % 2 == 0) {
          answer = s.substring(point - 1, point + 1);
      } else {
          answer = s.substring(point, point + 1);
      } 
      
      return answer;
  }
}
